{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "build": "npm run clean && tsc && cp package.json ./build && cp package-lock.json ./build && cp yarn.lock ./build && npm install --production --prefix build",
    "clean": "rm -rf ./build",
    "snyk-protect": "snyk protect",
    "prepare": "npm run snyk-protect"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.15.0",
    "@google-cloud/storage": "^1.1.1",
    "express": "^4.15.3",
    "firebase-admin": "~5.5.0",
    "firebase-functions": "^0.7.0",
    "flat": "^4.0.0",
    "lodash": "^4.17.4",
    "snyk": "^1.69.7"
  },
  "devDependencies": {
    "@types/express": "^4.0.37",
    "@types/firebase": "^3.2.1",
    "@types/flat": "^0.0.28",
    "@types/google-cloud__storage": "^1.1.5",
    "@types/lodash": "^4.14.74",
    "@types/node": "^9.3.0",
    "firebase-tools": "^3.12.0",
    "typescript": "^2.5.3"
  },
  "private": true,
  "snyk": true
}
